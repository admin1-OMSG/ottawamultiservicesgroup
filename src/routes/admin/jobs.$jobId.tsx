import { useEffect, useState } from "react"
import { createFileRoute, Link, useNavigate } from "@tanstack/react-router"
import { requireActiveAdmin } from "@/features/admin/requireAdmin"
import { formatDate } from "@/features/admin/formatters"
import { supabase } from "@/lib/supabase"
export const Route=createFileRoute("/admin/jobs/$jobId")({component:JobDetailPage})
type Job={id:string;job_number:string;title:string;description:string|null;status:string;priority:string;scheduled_start:string|null;scheduled_end:string|null;address_line:string|null;city:string|null;province:string|null;postal_code:string|null;internal_notes:string|null;customer:{first_name:string;last_name:string|null;email:string;phone:string|null}|null;employee:{id:string;first_name:string;last_name:string|null}|null}
type Employee={id:string;first_name:string;last_name:string|null}
const statuses={unscheduled:"À planifier",scheduled:"Planifié",in_progress:"En cours",completed:"Terminé",cancelled:"Annulé"}
function JobDetailPage(){const {jobId}=Route.useParams();const navigate=useNavigate();const [job,setJob]=useState<Job|null>(null);const [employees,setEmployees]=useState<Employee[]>([]);const [status,setStatus]=useState("");const [employeeId,setEmployeeId]=useState("");const [notes,setNotes]=useState("");const [error,setError]=useState("");const [success,setSuccess]=useState("");const [saving,setSaving]=useState(false);useEffect(()=>{void load()},[jobId]);async function load(){try{if(!(await requireActiveAdmin())){await navigate({to:"/admin/login"});return}const [j,e]=await Promise.all([supabase.from("jobs").select("id,job_number,title,description,status,priority,scheduled_start,scheduled_end,address_line,city,province,postal_code,internal_notes,customer:customers(first_name,last_name,email,phone),employee:employees(id,first_name,last_name)").eq("id",jobId).maybeSingle(),supabase.from("employees").select("id,first_name,last_name").eq("status","active").order("first_name")]);if(j.error)throw j.error;if(!j.data)throw new Error("Intervention introuvable.");const typed=j.data as unknown as Job;setJob(typed);setStatus(typed.status);setEmployeeId(typed.employee?.id||"");setNotes(typed.internal_notes||"");setEmployees((e.data??[]) as Employee[])}catch(e){setError(e instanceof Error?e.message:"Chargement impossible.")}}async function save(){if(!job)return;setSaving(true);setError("");setSuccess("");try{const user=await requireActiveAdmin();if(!user)return;const {error}=await supabase.from("jobs").update({status,assigned_employee_id:employeeId||null,internal_notes:notes||null,updated_by:user.id,updated_at:new Date().toISOString()}).eq("id",job.id);if(error)throw error;setSuccess("Intervention mise à jour.");await load()}catch(e){setError(e instanceof Error?e.message:"Mise à jour impossible.")}finally{setSaving(false)}}if(!job)return <p>{error||"Chargement…"}</p>;return <div className="space-y-6"><header><Link to="/admin/jobs" className="text-sm font-semibold text-blue-700">← Retour aux interventions</Link><h1 className="mt-2 text-3xl font-bold">{job.job_number}</h1><p className="text-slate-600">{job.title}</p></header>{error&&<div className="rounded-lg border border-red-200 bg-red-50 p-4 text-red-800">{error}</div>}{success&&<div className="rounded-lg border border-emerald-200 bg-emerald-50 p-4 text-emerald-800">{success}</div>}<div className="grid gap-6 lg:grid-cols-[1fr_340px]"><div className="space-y-6"><section className="rounded-xl border bg-white p-5 shadow-sm"><h2 className="text-lg font-bold">Client et lieu</h2><div className="mt-4 grid gap-4 sm:grid-cols-2"><Info label="Client" value={job.customer?`${job.customer.first_name} ${job.customer.last_name??""}`:null}/><Info label="Téléphone" value={job.customer?.phone||null}/><Info label="Courriel" value={job.customer?.email||null}/><Info label="Adresse" value={[job.address_line,job.city,job.province,job.postal_code].filter(Boolean).join(", ")}/></div></section><section className="rounded-xl border bg-white p-5 shadow-sm"><h2 className="text-lg font-bold">Travail</h2><p className="mt-3 whitespace-pre-wrap text-slate-700">{job.description||"Aucune description."}</p></section></div><aside className="space-y-6"><section className="rounded-xl border bg-white p-5 shadow-sm"><h2 className="font-bold">Planification</h2><dl className="mt-4 space-y-3 text-sm"><Info label="Début" value={job.scheduled_start?formatDate(job.scheduled_start):"À planifier"}/><Info label="Fin" value={job.scheduled_end?formatDate(job.scheduled_end):"—"}/></dl></section><section className="space-y-4 rounded-xl border bg-white p-5 shadow-sm"><h2 className="font-bold">Gestion</h2><label className="block text-sm font-medium">Statut<select value={status} onChange={e=>setStatus(e.target.value)} className="mt-1 w-full rounded-lg border px-3 py-2">{Object.entries(statuses).map(([v,l])=><option key={v} value={v}>{l}</option>)}</select></label><label className="block text-sm font-medium">Employé<select value={employeeId} onChange={e=>setEmployeeId(e.target.value)} className="mt-1 w-full rounded-lg border px-3 py-2"><option value="">Non affecté</option>{employees.map(x=><option key={x.id} value={x.id}>{x.first_name} {x.last_name??""}</option>)}</select></label><label className="block text-sm font-medium">Notes internes<textarea value={notes} onChange={e=>setNotes(e.target.value)} className="mt-1 w-full rounded-lg border px-3 py-2 min-h-28"/></label><button onClick={()=>void save()} disabled={saving} className="w-full rounded-lg bg-blue-700 px-4 py-2.5 font-semibold text-white disabled:opacity-50">{saving?"Enregistrement…":"Enregistrer"}</button></section></aside></div></div>}
function Info({label,value}:{label:string;value:string|null}){return <div><dt className="text-sm text-slate-500">{label}</dt><dd className="mt-1 font-medium">{value||"—"}</dd></div>}
